import React from 'react';

// Local Imports
import styles from './styles.module.scss';

type Props = {};

const MainContainer: React.FC<Props> = props => {
  return <h1>MainContainer</h1>;
};

export default MainContainer;
